class IntegerArrayMax is
    method MaxInt(a: Array[Integer]) : Integer is
        var max : Integer(100000)
        var i : Integer(1)
        while i.Less(10) loop
            if i.Greater(max) then max := Integer(2) end
            i := i.Plus(1)
        end
        return max
    end
end
